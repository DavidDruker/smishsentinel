# Pinned to the exact versions this project was built and tested against.
# Reproducibility over freshness: `pip install -r requirements.txt` should
# give a judge or a clean-clone the same behavior this was verified with, not
# whatever shipped upstream that week.
bedrock-agentcore==1.22.0
strands-agents==1.52.0
boto3==1.43.76
pydantic==2.13.4
scikit-learn==1.9.0
joblib==1.5.3
